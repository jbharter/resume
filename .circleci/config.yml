version: 2
jobs:
  build:
    docker:
      - image: jbharter/resume
    steps:
      - checkout
      - run:
          name: LaTeX build
          command: make pdf
      - store_artifacts:
          path: resume.pdf